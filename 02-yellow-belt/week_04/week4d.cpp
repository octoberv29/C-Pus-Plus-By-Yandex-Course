#include <iostream>
#include <vector>

using namespace std;


int main() {

// поиск в неотсортированном векторе
// find(begin(v), end(v), x)
// find_if(begin(v), end(v), [](int y) {...})
// count(begin(v), end(v), x)
// перебрать все можно с помощью цикла и fin


  return 0;
}
